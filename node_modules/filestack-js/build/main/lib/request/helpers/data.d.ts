import { FsRequestOptions, FsResponse } from './../types';
/**
 * Prepare request and set content-type header based on data
 *
 * @param headers
 * @param data
 */
export declare const prepareData: (config: FsRequestOptions) => FsRequestOptions;
/**
 * Add filestack debug headers to request
 *
 * @param config
 */
export declare const filestackHeaders: (config: FsRequestOptions) => FsRequestOptions;
/**
 * Prepare response data based on content type
 *
 * @param response
 */
export declare const parseResponse: (response: FsResponse) => Promise<FsResponse>;
