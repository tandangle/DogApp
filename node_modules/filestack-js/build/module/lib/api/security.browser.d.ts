import { Security } from './../client';
import { SecurityOptions, WebhookValidatePayload } from './security';
export declare const getSecurity: (policyOptions: SecurityOptions, appSecret: string) => Security;
export declare const validateWebhookSignature: (secret: string, rawBody: string, toCompare: WebhookValidatePayload) => never;
