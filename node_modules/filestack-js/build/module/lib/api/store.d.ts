import { Security, Session } from '../client';
import { StoreParams } from './../filelink';
import { UploadTags } from './upload/file';
export declare type StoreUrlParams = {
    session: Session;
    url?: string;
    storeParams?: StoreParams;
    token?: any;
    security?: Security;
    uploadTags?: UploadTags;
};
/**
 * Store given url with options and
 *
 * @param session
 * @param url
 * @param storeOpts
 * @param token
 * @param security
 * @param uploadTags
 */
export declare const storeURL: ({ session, url, storeParams, token, security, uploadTags, }: StoreUrlParams) => Promise<any>;
