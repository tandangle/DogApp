import { FsTokenInterface } from './types';
/**
 * Filestack token that allow pause, resume or cancel given upload
 *
 * @export
 * @class FsToken
 * @extends {EventEmitter}
 * @implements {FsTokenInterface}
 */
export declare class FsCancelToken implements FsTokenInterface {
    private source;
    private cancelMethod;
    constructor();
    /**
     * Cancel request action
     *
     * @param {(string | Error)} [reason]
     * @memberof Token
     */
    cancel(reason?: string | Error): void;
    /**
     * Returns cancel token promise
     *
     * @returns
     * @memberof Token
     */
    getSource(): Promise<string>;
}
