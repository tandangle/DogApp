import { FsRequestError } from './../error';
/**
 * Indicates if method should be retried
 *
 * @private
 * @param {FsRequestError} err
 * @returns
 * @memberof Dispatch
 */
export declare const shouldRetry: (err: FsRequestError) => boolean;
